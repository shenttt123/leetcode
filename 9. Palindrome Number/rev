def isPalindrome(x: int) -> bool:
    temp,rev=x,0
    while(x>0):
        dig=x%10
        rev=rev*10+dig
        x=x//10
        print(dig,rev,x)

    if(temp==rev):
        return True
    else:
        return False
